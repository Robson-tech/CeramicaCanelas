@page "/home"
 
 <div class="main-content">
     <NavMenu />
         <div id="form-container"></div>
    <div class="main-content">
        <div class="welcome-card" id="welcome-message">
            <h1 class="welcome-title">Bem-vindo ao Sistema Cerâmica Canelas</h1>
            <p class="welcome-text">Selecione uma opção no menu lateral para começar</p>
            <div id="form-container"></div>
    <style>
        /* Reset e Base */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: #f5f5f5;
    color: #333;
    line-height: 1.6;
}

/* Container Principal */
#form-container {
    max-width: 800px;
    margin: 20px auto;
    padding: 20px;
    background: white;
    border-radius: 10px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
}

/* Títulos - TODOS LARANJAS */
h2 {
    color: #ff6b35;
    text-align: center;
    margin-bottom: 30px;
    font-size: 28px;
    font-weight: 600;
    position: relative;
}

h2::after {
    content: '';
    display: block;
    width: 60px;
    height: 3px;
    background: #ff6b35;
    margin: 10px auto 0;
    border-radius: 2px;
}

/* Formulários */
.product-form,
.employee-form,
.user-form,
.category-form,
.suppliers-form {
    max-width: 100%;
}

/* Labels */
label {
    display: block;
    margin-bottom: 20px;
    font-weight: 500;
    color: #555;
    font-size: 16px;
    cursor: pointer;
}

/* Inputs, Selects e Textareas */
input[type="text"],
input[type="number"],
input[type="date"],
input[type="password"],
select,
textarea {
    width: 100%;
    padding: 12px 15px;
    border: 2px solid #ddd;
    border-radius: 8px;
    font-size: 16px;
    margin-top: 8px;
    transition: all 0.3s ease;
    background: #fafafa;
}

/* Estados de Focus - TODOS COM COR LARANJA */
input:focus,
select:focus,
textarea:focus {
    outline: none;
    border-color: #ff6b35;
    background: white;
    box-shadow: 0 0 0 3px rgba(255, 107, 53, 0.1);
}

/* Textarea específico */
textarea {
    resize: vertical;
    min-height: 100px;
    font-family: inherit;
}

/* Select customizado */
select {
    cursor: pointer;
    appearance: none;
    background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e");
    background-position: right 12px center;
    background-repeat: no-repeat;
    background-size: 16px;
    padding-right: 40px;
}

/* TODOS OS BOTÕES LARANJAS */
button[type="submit"] {
    width: 100%;
    padding: 15px;
    background: linear-gradient(135deg, #ff6b35, #e55a2b);
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 18px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 20px;
}

button[type="submit"]:hover {
    background: linear-gradient(135deg, #e55a2b, #cc4a1f);
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(255, 107, 53, 0.3);
}

button[type="submit"]:active {
    transform: translateY(0);
}

/* Quebras de linha removidas */
br {
    display: none;
}

/* Layout responsivo simples */
@* @media (max-width: 768px) {
    #form-container {
        margin: 10px;
        padding: 15px;
    }

    h2 {
        font-size: 24px;
    }

    input, select, textarea {
        font-size: 16px;
    }

    button[type="submit"] {
        font-size: 16px;
        padding: 12px;
    }
} *@

/* Animação suave */
.product-form,
.employee-form,
.user-form,
.category-form,
.suppliers-form {
    animation: fadeIn 0.5s ease-in;
}

@* @keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
} *@

/* Melhorias visuais */

/* Placeholder styling */
::placeholder {
    color: #999;
    opacity: 1;
}

/* Hover nos inputs */
input:hover:not(:focus),
select:hover:not(:focus),
textarea:hover:not(:focus) {
    border-color: #bbb;
}

/* Formulários acabam aqui */
    </style>
        </div>
    </div>
    </div>
    <script>
        
    function loadProductForm() {
        document.getElementById('welcome-message').style.display = 'none';

        const formHTML = `
            <h2>Cadastro de Produto</h2>
            <form class="product-form">
        
                <label>Codigo Do Produto: <input type="text" name="sku"></label><br>
                <label>Nome: <input type="text" name="nome"></label><br>
                <label>Unidade de Medida: 
                    <select name="unidade">
                        <option value="unidade">Unidade</option>
                        <option value="kg">Kg</option>
                        <option value="metro">Metro</option>
                        <option value="litro">Litro</option>
                    </select>
                </label><br>
                <label>Estoque Mínimo: <input type="number" name="estoque_minimo"></label><br>
                <label>Estoque Inicial: <input type="number" name="estoque_inicial"></label><br>
                <label>Valor Unitário: <input type="number" step="0.01" name="valor_unitario"></label><br>
                <label>Data de Cadastro: <input type="date" name="data_cadastro"></label><br>
                <label>Categoria: <input type="text" name="categoria"></label><br>
                <label>Item Devolvível?
                    <select name="devolvivel">
                        <option value="sim">Sim</option>
                        <option value="nao">Não</option>
                    </select>
                </label><br>
                <label>Observações: <textarea name="observacoes" rows="3"></textarea></label><br>
                <button type="submit">Salvar Produto</button>
            </form>
        `;

        document.getElementById('form-container').innerHTML = formHTML;
    }

    // Inicialização
    document.addEventListener('DOMContentLoaded', function() {
        const submenu = document.getElementById('almoxarifado-submenu');
        const icon = document.querySelector('.expand-icon');
        submenu.classList.add('expanded');
        icon.classList.add('rotated');

        document.querySelector('.menu-item').classList.add('active');
    });
    function loadEmployeeForm() {
    document.getElementById('welcome-message').style.display = 'none';

    const formHTML = `
        <h2>Cadastro de Funcionário</h2>
        <form class="employee-form">

            <label>Nome Completo: <input type="text" name="nome_completo"></label><br>
            <label>Cargo: <input type="text" name="cargo"></label><br>
            <label>Setor: 
                <select name="setor">
                    <option value="producao">Produção</option>
                    <option value="manutencao">Manutenção</option>
                    <option value="expedicao">Expedição</option>
                    <option value="administrativo">Administrativo</option>
                </select>
            </label><br>
            <label>CPF: <input type="text" name="cpf"></label><br>
            <label>Matrícula: <input type="text" name="matricula"></label><br>
            <label>Status:
                <select name="status">
                    <option value="ativo">Ativo</option>
                    <option value="inativo">Inativo</option>
                </select>
            </label><br>
            <button type="submit">Salvar Funcionário</button>
        </form>
    `;

    document.getElementById('form-container').innerHTML = formHTML;
}
function loadUserForm() {
    document.getElementById('welcome-message').style.display = 'none';

    const formHTML = `
        <h2>Cadastro de Usuário do Sistema</h2>
        <form class="user-form" onsubmit="event.preventDefault(); encryptPassword(this);">
            <label>Nome de Usuário: <input type="text" name="username" required></label><br>
            <label>Senha: <input type="password" name="senha" required></label><br>
            <label>Permissão:
                <select name="permissao">
                    <option value="admin">Admin</option>
                    <option value="operador">Operador</option>
                    <option value="leitura">Somente Leitura</option>
                </select>
            </label><br>
            <button type="submit">Salvar Usuário</button>
        </form>
    `;

    document.getElementById('form-container').innerHTML = formHTML;
}
function loadCategoryForm() {
    document.getElementById('welcome-message').style.display = 'none';

    const formHTML = `
        <h2>Cadastro de Categoria</h2>
        <form class="category-form" onsubmit="event.preventDefault(); alert('Categoria salva!');">
            <label>Nome da Categoria: <input type="text" name="category" required></label><br>
            <label>Descrição: <textarea name="description" rows="3"></textarea></label><br>
            <button type="submit">Salvar Categoria</button>
        </form>
    `;

    document.getElementById('form-container').innerHTML = formHTML;
}
function loadSuppliersForm() {
    document.getElementById('welcome-message').style.display = 'none';

    const formHTML = `
        <h2>Cadastro de Fornecedores</h2>
        <form class="suppliers-form">
            <label>Nome/Razão Social: <input type="text" name="nome_supplie"></label><br>
            <label>CNPJ: <input type="text" name="cnpj"></label><br>
            <label><span>Contato: <input type="text" name="telefone"></span></label><br>
            <label>Endereço: <input type="text" name="endereco"></label><br>
            <label>Produtos Fornecidos: <input type="text" name="produtos_fornecidos"></label><br>
            </label><br>
            <button type="submit">Salvar Fornecedor</button>
        </form>
    `;

    document.getElementById('form-container').innerHTML = formHTML;
}
    </script>